import { Leaf } from 'parchment/dist/src/blot/abstract/blot';
import ShadowBlot from 'parchment/dist/src/blot/abstract/shadow';
import * as Registry from 'parchment/dist/src/registry';
declare class LeafBlot extends ShadowBlot implements Leaf {
    static scope: Registry.Scope;
    static value(domNode: Node): any;
    index(node: Node, offset: number): number;
    position(index: number, inclusive?: boolean): [Node, number];
    value(): any;
}
export default LeafBlot;
